@page "/mobile/clock"
@attribute [Authorize]
@inject NavigationManager Nav

@*
    DEPRECATED 2026-04-21 — /mobile/clock was merged into /mobile/checkin as part
    of the mobile-unification work. The check-in page now dual-writes AttendanceTransaction
    + TcTimePunch for hourly / PT / sub employees, so one page serves both safety and payroll.
    This stub exists only to forward any surviving bookmark or deep link. Safe to `git rm`.
*@

@code {
    protected override void OnInitialized()
    {
        Nav.NavigateTo("/mobile/checkin", forceLoad: false, replace: true);
    }
}
